/**
 * 兼容旧 import；实现已迁到系统包 @fengyun/browser-shot。
 * 新插件请用 ctx.shot.renderMenu / renderHtml。
 */
export {
  menuHtml,
  renderMenuShot,
  renderHtmlShot,
  type ShotResult,
  type ShotMenuOpts,
  type ShotHtmlOpts,
} from "@fengyun/browser-shot";
