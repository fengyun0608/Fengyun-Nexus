---
description: Fengyun Nexus 仓库边界与开发约定。改本框架时必须遵守。
globs:
  - "**/*"
---

# Fengyun Nexus 规则

- 只修改 `Fengyun-Nexus/` 本仓库内容。
- 禁止修改工作区内 `AI-MultiUser-Core`、`XRK-plugin`、`USB-Portable-Workbench` 等其它项目。
- 对外文档保持产品语气；不要在 README 堆砌技术栈清单或冗长更新日志。
- **永远不要提交或推送用户本地配置与密钥**：`.env`、`.env.*`、`configs/*.local.json`、`data/`。
- **对外文档禁止写远程插件源路径、通行证变量名、registry 文件细节**；此类内容只放 `docs/internal/`。
- 默认启动只提供 **唯一控制台** `:8787`，不要再把第二套开发页写成主入口。
- UI 避免蓝紫渐变套路；沿用墨色 + 琥珀/鼠尾草绿视觉。
